server:
  port: 7777
spring:
  application:
    name: springconfigserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SmitMistry01/microservices_config_server.git
          clone-on-start: true
management:
  endpoints:
    web:
      exposure:
        include: "*"