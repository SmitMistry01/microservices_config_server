server:
 port: 7708

spring:
 datasource:
   url: jdbc:mysql://localhost:3306/servicedb
   username: root
   password: smit

 jpa:
   hibernate:
     ddl-auto: update
   show-sql: true

management:
 endpoints:
   web:
     exposure:
       include: "*"
