server:
  port: 7078
spring:
  datasource:
    url: 'jdbc:mysql://localhost:3306/orderdb'
    username: root
    password: smit
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
management:
  endpoints:
    web:
      exposure:
        include: '*'
eureka:
  client:
    service-url:
      defaultZone: 'http://localhost:7779/eureka/'
