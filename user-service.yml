server:
  port: 7077

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userservicedb
    username: root
    password: Bhanu123@

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
